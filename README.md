# Event-Driven-System

# important uv commands wrt microservices
# uv init 
# uv init --app services/user-service --> creating a separate toml file for the each microservices 
# uv add "fastapi[standard]" --package user-service  -> to install all the dependencies of the monorepo all from the services folder